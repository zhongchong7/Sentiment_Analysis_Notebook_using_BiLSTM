# Sentiment_Analysis_Notebook_using_BiLSTM
This project trains a sentiment classifier on the IMDB dataset using a BiLSTM with GloVe word embeddings. The notebook includes data loading, preprocessing, training with a train/validation split, and loss/accuracy plots.
